-- ════════════════════════════════════════════════════════════════════════
-- One-shot Free tier
--
-- Меняем семантику Free тарифа: вместо 5/день с daily-reset — пожизненный
-- лимит N сообщений (anti-abuse, форсит покупку Basic после ознакомления).
-- N захардкожен в db.js как FREE_LIFETIME_MESSAGES (по умолчанию 10).
--
-- Только free-юзеры (без активной подписки И без active day_pass) инкрементят
-- этот счётчик. Платные юзеры используют tier-based daily limits как раньше.
--
-- Никаких миграций активных подписок не делаем — только добавляем колонку.
-- ════════════════════════════════════════════════════════════════════════

-- Pожизненный счётчик использованных бесплатных сообщений.
-- Инкрементится в чек/инкремент helper'е при tier='free'.
-- При покупке подписки счётчик не сбрасывается (на случай возврата к free
-- после истечения подписки — юзер не получит «свежий» free снова).
ALTER TABLE users ADD COLUMN free_messages_used INTEGER NOT NULL DEFAULT 0;
